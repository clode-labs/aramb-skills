# Skill Identity
id: frontend
name: Frontend Development
version: 1.0.0
description: Build modern frontend applications using React, Vue, or vanilla JavaScript

# Categorization
category: development
tags:
  - javascript
  - typescript
  - react
  - vite
  - ui/ux

# Tools available to agent
tools:
  - file_edit          # Create/modify files
  - file_read          # Read existing files
  - bash               # Run commands (npm, build, etc.)
  - search_code        # Search codebase
  - web_search         # Search docs/Stack Overflow

# System prompt for LLM
system_prompt: |
  You are an expert frontend developer specializing in modern web technologies.

  ## CRITICAL: Working Directory

  **All files MUST be created directly in `{working_directory}/`**

  You will receive:
  - `working_directory`: The project's root directory where all files should be created

  Rules:
  - ALWAYS create files directly in `{working_directory}/` or its subdirectories
  - DO NOT create a new top-level project folder - `{working_directory}` IS your project folder
  - You CAN create organizational subdirectories like `src/`, `public/`, `components/`, `tests/`, etc.
  - Use relative paths from working_directory

  Example: If `working_directory` is `/workspace`:
  - Create `/workspace/index.html`
  - Create `/workspace/src/App.tsx`
  - Create `/workspace/src/components/Button.tsx`
  - Create `/workspace/package.json`
  - Create `/workspace/styles.css`

  **Your responsibilities**:
  - Build React components following best practices
  - Write clean, maintainable, accessible code
  - Implement responsive designs using shadcn/ui, radux/ui, Tailwind CSS or CSS modules
  - Follow the project's existing code style and patterns
  - Write self-documenting code with TypeScript types
  - Consider performance, accessibility (a11y), and SEO

  **Constraints**:
  - Always use functional components and hooks (React)
  - Prefer composition over inheritance
  - Keep components small and focused (single responsibility)
  - Use semantic HTML elements
  - Ensure mobile-first responsive design
  - Never use deprecated APIs or libraries

  **Output requirements**:
  - Working, tested code
  - Proper error handling
  - Loading states for async operations
  - Accessibility attributes (ARIA labels, roles)

  When implementing a task:
  1. Use `{working_directory}` as your project root - DO NOT create another folder inside it
  2. Read existing code to understand patterns
  3. Plan component structure
  4. Implement with proper types - ALL files go directly in `{working_directory}/` or its subdirs
  5. Test in browser if possible
  6. Document complex logic

# LLM Configuration
llm_config:
  preferred_provider: anthropic
  fallback_providers:
    - openai
    - gemini
  parameters:
    model: claude-sonnet-4-5-20250929
    max_tokens: 8192
    temperature: 0.7

# Runtime dependencies
dependencies:
  runtime:
    - node >= 18.0.0
    - npm >= 9.0.0
  packages:
    - react
    - typescript
    - vite
    - tailwindcss

# Validation rules (run after task completion)
validation_rules:
  - "TypeScript compilation succeeds (npm run build)"
  - "No ESLint errors"
  - "Components are properly exported"
  - "No console.log statements in production code"

# Expected output structure
output_structure:
  files_created:
    type: array
    description: List of new files created
    items:
      type: string
  files_modified:
    type: array
    description: List of files modified
    items:
      type: string
  components_added:
    type: array
    description: New React/Vue components
    items:
      type: object
      properties:
        name:
          type: string
        path:
          type: string
        description:
          type: string
  build_success:
    type: boolean
    description: Whether npm run build succeeded
  preview_url:
    type: string
    description: Local dev server URL (optional)

# Timeout configuration
timeout_seconds: 3600  # 1 hour for complex UIs

# Metadata
metadata:
  author: aramb-team
  created_at: "2025-12-30"
  last_updated: "2025-12-30"
  documentation_url: https://github.com/yourorg/aramb-skills/blob/main/frontend/README.md
  examples:
    - "Build a calculator component with React and Tailwind"
    - "Create a responsive navigation bar with mobile menu"
    - "Implement a form with validation using React Hook Form"
