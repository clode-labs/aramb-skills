# Skill Identity
id: backend
name: Backend Development
version: 1.0.0
description: Build backend services, APIs, and server-side applications

# Categorization
category: development
tags:
  - backend
  - api
  - server
  - go
  - node
  - python

# Tools available to agent
tools:
  - file_edit
  - file_read
  - bash
  - search_code
  - web_search

# System prompt for LLM
system_prompt: |
  You are an expert backend developer specializing in server-side development.

  **Your responsibilities**:
  - Build scalable backend services
  - Design and implement APIs (REST, GraphQL, gRPC)
  - Work with databases (SQL and NoSQL)
  - Implement authentication and authorization
  - Write efficient, secure, and maintainable code
  - Follow the project's existing patterns and conventions

  **Constraints**:
  - Always validate input data
  - Implement proper error handling
  - Use parameterized queries (never string concatenation for SQL)
  - Follow security best practices (OWASP guidelines)
  - Write idiomatic code for the language in use
  - Include logging for debugging and monitoring

  **Output requirements**:
  - Working, tested code
  - Input validation
  - Error handling with appropriate status codes
  - Database migrations (if applicable)
  - API documentation

  When implementing a task:
  1. Read existing code to understand patterns
  2. Design data models and API contracts
  3. Implement with proper validation
  4. Add error handling
  5. Write tests for critical paths
  6. Document the API

# LLM Configuration
llm_config:
  preferred_provider: anthropic
  fallback_providers:
    - openai
    - gemini
  parameters:
    model: claude-sonnet-4-5-20250929
    max_tokens: 8192
    temperature: 0.6

# Runtime dependencies
dependencies:
  runtime:
    - go >= 1.21 || node >= 18 || python >= 3.11
  packages: []

# Validation rules
validation_rules:
  - "Code compiles/runs without errors"
  - "All tests pass"
  - "No security vulnerabilities (SQL injection, etc.)"
  - "API endpoints return valid responses"

# Expected output structure
output_structure:
  files_created:
    type: array
    items:
      type: string
  files_modified:
    type: array
    items:
      type: string
  endpoints_added:
    type: array
    items:
      type: object
      properties:
        method:
          type: string
        path:
          type: string
        description:
          type: string
  migrations_created:
    type: array
    items:
      type: string
  tests_added:
    type: boolean

# Timeout configuration
timeout_seconds: 3600

# Metadata
metadata:
  author: aramb-team
  created_at: "2025-12-30"
  last_updated: "2025-12-30"
  examples:
    - "Create a user authentication API with JWT"
    - "Build a CRUD API for managing products"
    - "Implement a webhook handler"
